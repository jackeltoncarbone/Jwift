# Jwift.Angular

Angular bindings for Jwift — Swift-style UI templates and components built on
top of Jaui. Provides toolbar, navigation, dropdown, and similar high-level
components that consume `jaui-angular` primitives.

## Build

```bash
ng build
```

Output goes to `dist/jwift-angular`.

## Test

```bash
ng test
```
