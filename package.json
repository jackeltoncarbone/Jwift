{
  "name": "jwift-workspace",
  "private": true,
  "description": "Jwift monorepo — Swift-style UI templates and components built on Jaui",
  "workspaces": [
    "Angular"
  ],
  "scripts": {
    "build": "npm run build --workspace=Angular",
    "test": "npm test --workspace=Angular",
    "watch": "npm run watch --workspace=Angular"
  }
}
